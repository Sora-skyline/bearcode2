# 待改进

## 1. `retrieved_reference` 可能错误锚定最高分 Skill

当前会把上一轮检索得分最高的 Skill 作为 `retrieved_reference`，供新经验的合并判断使用。但“检索分数最高”不等于“真正相关或已被使用”，可能导致 Maintainer 被错误锚定，将新经验合并到不相关的 Skill。

更稳妥的做法是只在该 Skill 已被判定为 `relevant`/`used` 后才传入 `retrieved_reference`；否则传 `None`，完全依靠候选经验重新检索相似 Skill。

## 2. `used` 是事后推测，不是可靠的使用证据

当前通过 side-query LLM 根据用户请求、最终回答和 Skill 摘要判断 `used`。这只能说明回答“看起来符合”某个 Skill，无法证明生成回答时真的使用了它。而且 judge 看到的 hits 不包含完整 Skill 指令；side query 不可用时还会退化为“回答中是否出现 Skill 名称”的弱启发式，容易产生误判。

应将“显式使用证据”和“事后行为推测”分开统计：Skill 被展开、调用或执行时直接记录 `used`；LLM judge 的结果另行记为 `inferred_used`。如果自动注入的 Skill 摘要无法生成显式轨迹，就不应将 judge 结果当作确定性使用证据。
